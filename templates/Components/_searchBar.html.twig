<form action="{{ path('bathroom') }}" method="get" class="form-search">
	<input class="search-bar" type="text" id="" name="location" placeholder="">
	<button class="btn">
		Chercher
	</button>
</form>